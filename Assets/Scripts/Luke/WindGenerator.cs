using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WindGenerator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Where does this component go? Need a wind object to attach this script to, where the designers can define the direction of the wind
        // Then you need a component for objects that can be affected by wind to put the movement logic
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
